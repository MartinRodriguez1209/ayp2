with Lista;

package data is

   procedure cargarTurnos (listaFinal : out lista.TipoLista);

   procedure cargarClientes (listaFinal : out lista.TipoLista);

   procedure cargarMecanicos (listaFinal : out lista.TipoLista);

   procedure cargarReparaciones (listaFinal : out lista.TipoLista);

end data;
