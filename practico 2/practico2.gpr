project practico2 is
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("usacola.adb");

   package Compiler is
      for Switches ("Ada") use ("-g", "-O0");
      end Compiler;
end practico2;
